#pragma once

#include <string>
#include <vector>

// Build-time generated from all runtime C++ string literals that contain
// user-visible non-ASCII text. Dynamic world names are appended by main.cpp.
std::vector<std::string> BuildUiTextCatalog();
