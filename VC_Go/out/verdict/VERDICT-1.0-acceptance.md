# VERDICT —— 1.0 契约验收判决书（重建版）

> # ⛔ 已失效（2026-09-20）
>
> **本文件不再作为结论来源。** 取代它的是 **`docs/verdict/1.0 验收判决书.md`**（2026-09-20 重审版）。
>
> **失效原因**（逐条见新判决书 §七）：
> 1. 本文写于 2026-09-19（R26 之前口径），"**A7/A8 从未被判定、汇总分母恒为 6**"已失效——
>    R39 起 `report.Assess` 判定 A1–A8、R42 再增 A9 ⇒ **现行 9 条**（`report_test.go:35` 断言 = 9）；
> 2. "**模拟不是逐位可复现的**（货币抖动 0.061%）"已失效——R72 由 `ledger.sortedAccounts()` 修复，
>    本次 6 次复跑逐位一致；
> 3. "残差源于漏掉 `GovSelfTax`"结论对但**成因错**——该字段已不存在，真因是漏
>    `−GovWelfare/−GovPublicWorks/−GovWarehouseExpand` 三项，且残差**每 tick 都在 −1.3e6 量级**；
> 4. "经济归零、首次归零 tick 894"已随 R15–R47 的规则松绑失效——现行默认参数 10,000 tick **不再归零**；
> 5. "§3.3 配方与 §8.6 的 L\* 表不相容"需重新定性——按同一张表逐格核对，
>    下游中间投入**恰好等于**上游产出。
>
> **保留原因**：按 `docs/ACTIVE.md` §五·1 的纪律，历史结论**不得静默替换**，
> 故本文原文保留备查；引用它时必须同时对照 `docs/verdict/1.0 验收判决书.md`。
>
> ---
>
> ## ⚠️ 重建声明（先读这段）
>
> - **原始判决书从未入库**：`out/` 在仓库根且被 `.gitignore` 忽略，`out/verdict/VERDICT-1.0-acceptance.md`
>   及其全部证据在仓库里没有副本。本文由 `docs/ACTIVE.md` 转述的判决要点 + **2026-09-19 在本机重新生成的证据**重建。
> - **数字来源标注**：`[重跑]` = 本次实跑/独立复算得到；`[文档]` = 仅见诸 `docs/`、本次**无法复算**；
>   `[修正]` = 文档里的数字被本次复算**推翻或改写**。
> - **配套文件**：证据清单与已知缺陷见 `EVIDENCE-CAVEATS.md`；机检断言表见
>   `claims-verified-js.txt`（JS 侧）与 `contract-check-cpp.txt`（C++ 侧，两套独立实现互验一致）。
> - **本判决书不含**原始判决书里可能存在的、`ACTIVE.md` 未转述的论证；凡本文未标注来源的推理均为本次重建时补写。

---

## 〇、判决

> **判决 1：1.0 契约未通过验收。**
> **判决 2：1.0 的模拟逻辑不能作为市场模拟游戏的核心逻辑。**

两条判决在本次重跑中**全部成立**，且其中若干条事实比原记录更严重（见 §八）。

**可以原样保留的正面资产**（本次重跑复核通过）：

| 资产 | 复核结果 |
|------|----------|
| §2 价格动力学（常弹性需求 + 二阶弹性系统 + RK4） | 谱半径 **0.500000** `[重跑]`（`sim-10000pop10m.txt` 首段） |
| §3.4 零利润价 / 加成价 | 11 个商品开局利润率**全部恰为 20.00%** `[重跑]` |
| §3.3 投入产出表 + §6.3 需求表 | 三表联合标定 `k = 1.040000` `[重跑]` |
| §4.5.3 唯一记账入口 | 货币守恒、借贷相等（0 违规）、政府分解残差 **0.00**、逐建筑对账残差 **0.00** `[重跑]` |
| §4.5 政府/资本骨架（税收、建造力买卖闭环、债务上限） | 机制闭环成立；**参数匹配失败**（§二） |
| Go 工程本身 | `go build` / `go vet` / `go test` 全绿；`DSH_AUDIT=1` 下 **33 通过 / 0 失败** `[重跑]` |

---

## 一、判决所依据的六项事实（逐条复核）

| # | 事实 | 本次复核 |
|---|------|----------|
| F1 | 契约 §8.4 列 8 条判据，`report.Assess` 只实现 A1–A6，A7/A8（唯一能识别系统死亡者）从未被判定 | **CONFIRMED** `[重跑]` |
| F2 | §3.3 配方与 §8.6 的 L\* 表不相容；上游四项须 2.0~3.1 倍 | **CONFIRMED** `[重跑]`，由 JS 与 C++ 两套独立实现互验 |
| F3 | 税率 0 → 200% 死亡形态不变 | **CONFIRMED 且更强** `[重跑]`：首次归零 tick 894 / 894 / **895** |
| F4 | A4 无判别力 | **CONFIRMED 且更强** `[重跑]`：同命令重复 10 次 **5 通过 / 5 未通过** |
| F5 | 模拟不是逐位可复现的 | **CONFIRMED** `[重跑]`：离散量完全一致，货币抖动 **0.061%**（原记录 0.01~0.04%） |
| F6 | `diag_govcash` tick 1「残差 −514,012.88」是工具缺陷而非记账漏洞 | **数值 CONFIRMED，成因修正** `[重跑]` → 见 F6 |

### F1：A7/A8 从未被判定 — CONFIRMED

`gosim/internal/report/report.go` 中构造 `Verdict` 的位置恰好 6 处，ID 依次为 `A1`(`:106`)、`A2`(`:143`)、
`A3`(`:165`)、`A4`(`:191`)、`A5`(`:211`)、`A6`(`:225`)，**没有 A7/A8**。而契约 §8.4 自己列了 8 条，
其中 A8（货币守恒）在本工程里恰好是唯一全程真正成立的判据——即"唯一能识别系统死亡的判据没被接上"。
`market-sim` 的汇总分母因此恒为 6，实测汇总为 3~4 / 6（见 §五）。

### F2：§3.3 与 §8.6 不相容 — CONFIRMED

按 §8.6 **自己列的**最终需求反解供求平衡所需等级（不动点迭代）：

| 商品 | §8.6 定案 L\* | 反解所需 L\* | 相对偏差 |
|------|---------------|--------------|----------|
| 谷物 / 加工食品 / 织物 / 服装 / 高档服装 / 住房 | 256.06 / 182.90 / 81.66 / 23.37 / 13.00 / 99.84 | 同值 | ≤ **0.017%** |
| 煤 | 7.56 | **18.12** | +139.7% |
| 铁 | 7.56 | **23.67** | +213.2% |
| 钢 | 7.56 | **15.34** | +102.9% |
| 工具 | 9.08 | **19.08** | +110.1% |
| 合计 | 688.59 | **733.05** | +6.45% |

⇒ 下游六项吻合到取整，**上游四项须 2.03~3.13 倍**（原记录 2.0~3.1 倍）。
独立复算：`tools/contract_consistency_probe.js`（JS）、`tools/cpp/contract_check.cpp`（C++ / long double）、
`tools/verify_claims.js`（JS）——**三份实现逐项一致（相对差 < 0.001%）**。
这直接解释了默认布点开局就有 `铁(0.371)` 的配给比（见 §四）：§8.6 的"平衡点"本身没给上游留够产能。

### F3：税率 0→200% 死亡形态不变 — CONFIRMED（比原记录更尖锐）

| 税率 | 600 tick 末总级数 | 首次归零 tick `[重跑]` |
|------|-------------------|------------------------|
| 0.00 | 10 | **894** |
| 0.10 | 10 | **894** |
| 2.00 | 11 | **895** |

税率提高 **20 倍**只把首次归零推后 **1 个 tick**。⇒「提高税率救经济」被证否。
（600 tick 末的政府现金池确实随税率单调改善：−4.84e8 → −4.40e7，但死亡时点几乎不动。）

### F4：A4 无判别力 — CONFIRMED（同命令 5/10 翻转）

`a4-flip-test.txt`：同一二进制、同一命令（`-ticks 10000`，pop 10m，t=0.10）重复 10 次，
A4（`GDP 全程 > 0 且末 500 周期斜率 ≥ 0`）**通过 5 次、未通过 5 次**。
原因：死亡态上 GDP 是常数平台（`最低 GDP=5,093,468`），拟合斜率是 O(1e-8) 的浮点噪声，符号不可复现。
原记录的说法是"10m 判通过、5m 判未通过"`[文档]`；本次给出的是**更强且可复现**的结论：
**同一参数的重复执行之间就会翻转**，该判据不承载任何信息。

### F5：非逐位可复现 — CONFIRMED

`determinism.txt`：`-ticks 52` 重复 5 次，离散量（总级数 84、人口 10,000,000）**完全一致**；
政府现金池 = −61,066,241 / −61,048,086 / −61,079,578 / −61,042,328 / −61,062,670，
极差 37,250，**相对极差 0.061%**（原记录 0.01~0.04% `[文档]`，本次略宽，同量级）。
⇒ 引用货币绝对值必须带量级口径；单次实跑的精确货币值不能作为验收证据。

### F6：`diag_govcash` tick 1 残差 — 数值 CONFIRMED，**成因修正**

`diag_govcash.txt` 复现出与原记录**逐位相同**的 tick 1 残差 **−514,012.88**，tick 2 起为 0.00。
但成因**不是**原记录的"主函数求和用未取负的字段、打印时取负，口径相反"`[修正]`：

- CLI 的分解式（`cmd/diag_govcash/main.go:66-67`）为
  `Δ = 税收 + 经营净额 − 采购支出 + 售力收入 − 自建付款`；
- `internal/sim/audit_govdecomp_test.go:26` 的分解式在同一位置多一项
  **`− GovSelfTax`（政府收自己的税）**，其三次 tick 的残差均为 **0.00** `[重跑]`；
- 两个式子其余各项逐字相同，故 CLI 的残差恒等于**被漏掉的那一项**：残差 = −GovSelfTax。

⇒ 这是工具**漏项**，不是记账漏洞（记账层逐建筑对账残差为 0.00）。修复方式：CLI 补上该项并在列中显示。

---

## 二、命题（甲）：s=0.70 与 t=0.10 结构性不匹配

**结论成立**：政府工资义务 = 0.70·W 与 10% 交易税**不是同一个量级**，与人口、与需求缩放 k、与起点布点无关。

### 重算后的正确数字（口径：§8.6 定案 L\*，建造部门 20 级）

| 量 | 本次复算 `[重跑]` | 文档记录 | 判定 |
|----|------------------|----------|------|
| 生产建筑总级数 | 688.59 级 | 688.61 | CONFIRMED |
| 工资总额 W | 23,239,912 元/周期 | — | — |
| 最终需求名义价值 | 23,238,843 | — | — |
| 平衡态税后覆盖率 `(W/1.1)÷最终需求` | **0.909133** | 0.909157 / 缺口 9.0843% | **CONFIRMED** |
| 税收 @ t=10% | 3,385,949 | — | — |
| **税收 / W** | **0.1457** | 0.095（"约 9.5%"） | **DIFFERS** `[修正]` |
| **政府工资义务覆盖率** | **20.81%**（三口径 19.03%~20.94%） | 约 9.5% | **DIFFERS** `[修正]` |
| **缺口 = 0.70W − 税收** | **0.5543·W**（12,881,990 元/周期） | 0.61·W | CLOSE（差 9.1%） |
| **t=10% 时的政府持股上限 s** | **≤ 0.1457** | ≤ 0.04 | **DIFFERS** `[修正]` |
| **s=0.70 时所需税率 t\*** | **≥ 48.05%**（契约税率的 4.8 倍） | — | — |
| **G7 债务上限覆盖缺口** | **0.34 个周期**（4,350,000 元） | 3~6 个周期 | **DIFFERS**（实际更糟）`[修正]` |

**为什么 9.5% / s≤0.04 不能复现**：那两个数字来自 `tools/survival_condition_probe.js` 的
**硬编码结论句**，而不是它的计算值——该脚本自己的计算给出的是 0.1389 / 0.5611W / s≤0.1389；
且它与 `contract_consistency_probe.js` 使用的 L\* 口径不同（建造部门固定 5 级 vs 20 级）。
详见 `EVIDENCE-CAVEATS.md` §三。

**第三处独立复算（仓库自带的可视化工具）**：`tools/viz/gen_dashboard.js` 从契约 §8.6 的 L\* 独立复算，
重跑打印 `政府缺口 0.5631 W；税收覆盖工资义务 19.55%；债务上限 435万（0.31 个周期）；
自洽所需税率 t* = 46.5%；或 t=10% 时 s 上限 = 0.1506`（`../viz/RERUN-VIZ.txt`）。
它与本次 C++/JS 复核一致，**同样不支持** 9.5% / s≤0.04 / 3~6 周期——
即修正后的数字在仓库里早已有一个独立实现给出，只是没被写回 `ACTIVE.md`。

**修正后结论不变**：政府每周期工资义务 0.70·W = **1,626.8 万元**，而 10% 交易税只有 **338.6 万元**，
覆盖率 **19.0%~20.9%**（三处独立复算 × 三种税基口径）——缺口是 W 的固定比例，**不可能靠调参补齐**。

---

## 三、命题（乙）：参数空间内不存在存活解

`sim-600-tax-sweep.txt`：11 组实跑（人口 10,000,000，600 tick），**全部走向死亡**。

| 起点布点 | 税率 | 起始级数 | 600 tick 末级数 | 末政府现金池 | 首次归零 tick |
|----------|------|----------|-----------------|--------------|---------------|
| 统一 5 级 | 0.00 | 84.0 | 10 | −483,856,557 | 894 |
| 统一 5 级 | 0.10 | 84.0 | 10 | −458,779,533 | 894 |
| 统一 5 级 | 0.20 | 84.0 | 10 | −434,451,528 | — |
| 统一 5 级 | 0.40 | 84.0 | 10 | −385,038,691 | — |
| 统一 5 级 | 0.70 | 84.0 | 10 | −313,401,612 | — |
| 统一 5 级 | 1.00 | 84.0 | 10 | −245,229,917 | — |
| 统一 5 级 | 1.50 | 84.0 | 10 | −131,374,087 | — |
| 统一 5 级 | 2.00 | 84.0 | 11 | −44,020,736 | 895 |
| 物质平衡 | 0.00 | 1435.6 | 147 | −7,825,110,974 | — |
| 物质平衡 | 0.10 | 1435.6 | 147 | −7,413,545,560 | 1334 |
| 物质平衡 + 私有化 | 0.10 | 1435.6 | 147 | −7,409,100,966 | **1319** |

三条读数：

1. **提高税率无效**（F3）：0% 与 200% 的首次归零只差 1 个 tick。
2. **换布点无效**：物质平衡布点起始 1435.6 级（默认的 17 倍、工资 17 倍），末态留存率反而更低（10.2% vs 12.0%），
   且政府债务规模大 16 倍（−7.4e9 vs −0.46e9）。
3. **私有化开关有可见但微小的效应** `[修正]`：首次归零 1334 → **1319**（提前 15 tick，1.1%）。
   原记录"对轨迹没有任何可见影响"`[文档]`在 600 tick 的粗采样下成立，在精确的首次归零口径下不成立。

⇒ 命题（乙）成立：**在契约给出的参数空间内不存在存活解。**

---

## 四、死亡机制：两条各自都足以致死的独立路径

`audit-survival-600.txt` 的"最紧投入(配给比)"列给出断链次序：

**路径 A（供给侧，默认布点）**

| tick | 总级数 | 最紧投入(配给比) | 事件 |
|------|--------|------------------|------|
| 1 | 84.0 | **铁(0.375)** | 开局即只有 37.5% 的申报投入被满足 |
| 100~400 | 84.4 | **铁(0.371)** | 长期结构性短缺，钢/工具被抽干 |
| 500 | 10.6 | **钢(0.000)** | 钢产出归零 ⇒ 建造/维修链断裂 |
| 600 | 10.1 | 钢(0.000) | 只剩 12.0% 留存 |
| **894** | **0** | — | 全部建筑缩编归零 `[重跑]` |

配给比 0.371 的**来源已由 F2 定量解释**：上游四部门在 §8.6 的平衡表里被低估 2.03~3.13 倍。
即便把税率拉到 200%（路径 B 的缺口压缩一个数量级），首次归零仍只从 894 推到 895 ⇒ **路径 A 足以独立致死**。

**路径 B（财政侧）**

政府工资义务覆盖率仅 20.8%（§二），缺口 0.554·W/周期，而 G7 债务上限只够 **0.34 个周期**：
政府现金池在 tick 52 已到 −6.10e7，tick 1000 到 −4.96e8 并**永久停在那里**。
物质平衡布点更极端：政府债务 −7.4e9，tick 500 时煤配给归零 ⇒ **路径 B 也足以独立致死**。

**死亡后的"假稳态"（冻结证据 `[重跑]`）**

`sim-10000pop10m.txt` 时序表：tick 1000 / 3000 / 5000 / 10000 的总级数恒为 **0**、建造力 **0**、
政府现金池恒为 **−495,999,996**、税收与全部资金流恒为 **0**。
⇒ 原记录"从 tick 1000 起完全冻结"在**级数、货币、流量**三个口径上成立；
**但人口不在冻结之列**：14,668,917（tick 1000）→ 3,047（3000）→ 1（5000）→ **0**（10000）`[修正]`。
所谓"A2/A3/A6 通过"正是发生在这个死态上（见 §五），即契约 §8.4 自己警告的**空真通过**。

---

## 五、§8.4 验收判据实测（`-ticks 10000`，人口 10m，t=0.10）

| 判据 | 判定 | 实测 | 备注 |
|------|------|------|------|
| A1 价格在合法带内且贴边 < 5% | **未通过** | 最大贴边率 **100.00%**，max\|ln(P/Pcost)\| = 1.6094（限 ln5 = 1.6094） | 判据自身缺陷：阈值与钳制上限重合，触边即取等号 |
| A2 利润率 ∈ [−10%, +25%] | 通过（**空真**） | min = max = 加权 = **0.00%** | 全部建筑归零，利润率恒 0 |
| A3 均衡点不漂移 | 通过（**空真**） | Δ(P/Pcost) = 0，Δ(S/a) = 0 | 死态不动；且与 §6.5 人口增长数学互斥 |
| A4 GDP > 0 且末 500 期斜率 ≥ 0 | **不稳定** | 记录样本：斜率 −5.025e-08 → 未通过；重复 10 次 **5 通过 / 5 未通过** | 判定由 1e-8 噪声决定（F4） |
| A5 无爆炸与无坍缩 | **未通过** | 人口年化 **−18.03%**，总级数 **84 → 0** | 唯二真正有判别力的判据之一 |
| A6 建造队列不饥饿 | 通过（**空真**） | 队列非空占比 0.0%，同时记录"总级数 84→0" | 判据自身允许用"级数在增长"绕过 |
| **A7 政府债务** | **未实现** | — | F1 |
| **A8 货币守恒** | **未实现** | （记账层单独复核：净变动 = 注入额，扣除后 Δ=0） | F1；唯一全程成立的判据却没接进验收 |

⇒ 汇总"3~4 / 6"**不可解读为经济健康**：通过项全部是死亡态上的空真通过。
契约指定的 10,000 周期实跑末态：**人口 0、总级数 0、政府现金池 −495,999,996、税收 0**。

---

## 六、未决事项与修复优先级

`docs/ACTIVE.md` §四列出的四项待裁决项（**本次未修改任何契约参数**）：

| # | 问题 | 可选方向 | 本次复算补充 |
|---|------|----------|--------------|
| 1 | 政府持股 70% vs 交易税 10% 不匹配（主因） | (a) 保持 10% 税 ⇒ s ≤ **0.1457**；(b) 保持 70% 持股 ⇒ t* ≥ **48.05%**；(c) 政府"只分红不担亏" | 数字按 §二 的复算更新，方向不变 |
| 2 | 债务上限基数只取建造力产出 | (a) 扩到政府全部资产；(b) 显式接受"周期性停摆"作为玩法 | 现口径只够 **0.34 个周期** |
| 3 | 需求缩放 k 未含税率 | 标定时并入税率，或把 k 并入需求表重标定 | 注意：**不要**整体上调需求表（无效） |
| 4 | 起点产能 / 平衡产能 = 1/9.8 | 需要"从短缺起步"的显式设计，保证起点利润率足以触发 §4.1 扩建阈值 | 默认布点开局 8/11 部门利润率为负 |

**修复优先级**（沿用原判决书的 P0/P1/P2，`ACTIVE.md` §判决末段转述）：

- **P0**：重做 §8.6 标定（F2 的上游四项须 2.03~3.13 倍）+ 补存活判据 + 接入 A7/A8（F1）。
- **P1**：重新匹配 s 与 t（§二）+ 扩大债务上限基数 + 把 k 并入税率。
- **P2**：外部部门与玩家决策接口（判决 2 的四条"没有"）。

---

## 七、复现清单

**工具链**：Go `D:\Code\Go\bin\go.exe`（1.26.5，在 PATH 上）；g++ `D:\Code\mingw64\bin\g++.exe`（16.1.0）；
JS 本机无 node/npm，用 DSH Desktop 的 Electron 以 node 模式运行（`ELECTRON_RUN_AS_NODE=1`，node v24.18.1）。

```powershell
# 0) 一键重跑全部证据（幂等）
powershell -ExecutionPolicy Bypass -File D:\Code\YehenalaMarket\out\verdict\rerun-all.ps1
powershell -ExecutionPolicy Bypass -File D:\Code\YehenalaMarket\out\verdict\rerun-part2.ps1

# 1) 工程自检（Go）
cd D:\Code\YehenalaMarket\gosim
go build ./... ; go vet ./... ; go test ./... -count=1
$env:DSH_AUDIT="1" ; go test ./internal/sim/ -v -count=1     # 33 通过 / 0 失败

# 2) 契约指定长跑与对照（产物：sim-10000pop10m.txt / sim-3000-init-level-0.txt / sim-600-tax-sweep.txt）
go run ./cmd/market-sim -ticks 10000
go run ./cmd/market-sim -ticks 3000 -init-level 0
go run ./cmd/market-sim -ticks 600 -tax 2.00
go run ./cmd/diag_govcash

# 3) 算术探针（JS，node 缺失时用 Electron）
$env:ELECTRON_RUN_AS_NODE="1"
& "D:\DSH\DSH Desktop\DSH Desktop.exe" tools\contract_consistency_probe.js
& "D:\DSH\DSH Desktop\DSH Desktop.exe" tools\survival_condition_probe.js   # ⚠ 结论句为硬编码，见 EVIDENCE-CAVEATS.md
& "D:\DSH\DSH Desktop\DSH Desktop.exe" tools\verify_claims.js

# 4) 独立复核器（C++）
g++ -O2 -std=c++17 -Wall -o out\bin\contract_check.exe tools\cpp\contract_check.cpp
out\bin\contract_check.exe > out\verdict\contract-check-cpp.txt
```

| 证据文件 | 支撑的结论 |
|----------|------------|
| `contract_consistency_probe.txt` + `contract-check-cpp.txt` + `claims-verified-js.txt` | F2、§二 的断言表（JS/C++ 互验一致） |
| `../viz/RERUN-VIZ.txt`（`gen_dashboard.js` 的输出） | §二 的第三处独立复算（0.5631W / 19.55% / s≤0.1506 / 0.31 周期 / t*=46.5%） |
| `sim-600-tax-sweep.txt` | F3、§三 的 11 组对照 |
| `first-zero-bisect.txt` | F3 的首次归零 tick（894 / 894 / 895 / 1334 / 1319） |
| `sim-10000pop10m.txt`、`sim-3000-init-level-0.txt` | §四 冻结证据、§五 A1–A6 |
| `a4-flip-test.txt` | F4（同命令 5/5 翻转） |
| `determinism.txt` | F5（货币抖动 0.061%） |
| `diag_govcash.txt` + `audit-govdecomp.txt` | F6（残差 −514,012.88 / 含自收税项时 0.00） |
| `audit-survival-600.txt` + `audit-layout.txt` + `audit-start-diag.txt` | §四 配给比、起点布点与购买力诊断 |
| `audit-suite.txt` | 33 通过 / 0 失败 |
| `../archive/2026-09-18-equilibrium/`（索引 + 19 个探针重跑） | §六 的既有结论出处、文档渲染与可视化证据 |

---

## 八、本次重建对原判决书的修正（逐条）

| # | 原记录 `[文档]` | 本次复算 `[重跑]` | 性质 |
|---|-----------------|-------------------|------|
| 1 | 税收只覆盖约 9.5% 产出 | **14.57%**（税收/W）；政府工资义务覆盖率 **20.8%** | 数字修正 |
| 2 | t=10% ⇒ 政府持股 s ≤ 0.04 | **s ≤ 0.1457** | 数字修正 |
| 3 | 债务上限只够 3~6 个周期 | **0.34 个周期** | 数字修正（实际更糟） |
| 4 | 缺口 ≈ 0.61·W | **0.554·W**（探针口径 0.561·W） | 近似（差 9.1%） |
| 5 | A4：10m 通过 / 5m 未通过 | **同命令重复即 5 通过 / 5 未通过** | 结论强化 |
| 6 | F3：600 tick 末级数 10~11 不变 | **首次归零 894 / 894 / 895** | 结论强化 |
| 7 | `diag_govcash` 残差源于"符号相反" | 源于**漏掉 `GovSelfTax` 项**；残差 = −GovSelfTax | 成因修正 |
| 8 | 私有化开关"没有任何可见影响" | 首次归零 1334 → **1319**（提前 1.1%） | 结论细化 |
| 9 | "从 tick 1000 起完全冻结" | 级数/货币/流量成立；**人口仍从 1,466.9 万衰减到 0** | 结论细化 |
| 10 | 两条独立致死机制 | **CONFIRMED**，且给出各自独立的量化证据（配给比 0.371/0.000 与债务 −4.96e8 / −7.4e9） | 复核确认 |

> 修正 1–4 的共同根因：原数字来自 `tools/survival_condition_probe.js` 的**硬编码结论句**与
> 它自成一体的 L\* 口径（建造部门 5 级），既未随计算更新，也与 `contract_consistency_probe.js`（20 级）不一致。
> 恢复该项目的后续会话在引用任何旧数字前，请先读 `EVIDENCE-CAVEATS.md` §三。
