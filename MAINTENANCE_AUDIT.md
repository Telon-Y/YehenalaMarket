# 维护审计与修复记录

> 日期：2026-09-15 ｜ 基线：`ba90874` ｜ 结果：`49d3caa`（含后续清理）
>
> 本文记录一次针对本仓库的"被忽视部分"审计，以及每一项的处理结果。审计结论先于修改得出，修改前已就编排、语义与范围逐项确认。所有条目都给出可复现的证据或测试。

## 1. 审计方法

- 直接运行 8 个测试可执行文件，不通过 `ctest` 的日志写入路径。
- 用 `--warmup 104 --dump-debug-state` 抓取约 20 MB 运行态 JSON，对货币供应、GDP、建造力、金融建筑等做量化统计。
- 逐文件阅读源代码；用脚本统计常量、字段、函数的引用数。
- 用 `git worktree` 从提交本身做一次全新 clone 的配置、构建与测试，作为"仓库可重建"的唯一判据。

## 2. 修复清单

| 编号 | 问题 | 处理 | 证据 |
| --- | --- | --- | --- |
| G1 | 国家基础建造力的付款从国库扣款后无人入账，货币凭空销毁 | `construction_service.cpp` 在基础分支调用 `LocalMarket::recordNationalConstructionBase`；新增回归测试 | 104 周标准世界：修复后货币供应多出约 **4.87e8**，与每周约 4.5e6 的销毁量吻合（104 周 ≈ 4.7e8）；移除该调用后回归测试以 `credit=0` 失败 |
| G2 | 公开 GDP 被下限化为 1e-3，使 `allGdpPositive` 结构性不可能失败并掩盖负增加值 | 新增未下限化的 `rawGdp` 与平行历史序列，报告暴露 `rawWeeklyGdp`/`rawAnnualizedGdp`，健康门槛改判原始值；`gdpStable` 参与退出码 | 修复前 `gdpHistory` 中出现 30 个 `0.001` 记录；修复后 69 个市场无一非正，门槛变为真实可失败 |
| G3 | 铁路距离是省份下标占位值，运输周期恒为 1 周，而文档声称"每周 500 km 折算" | **按确认结论只更正文档**：删去 500 km 说法，明确记录占位距离与恒定周期 | `FUNCTIONAL_AUDIT.md` 9.3 节、`README.md` 规则 5 |
| G4 | "仓库利润"结构性恒为 0，却在运输页、宏观页展示 | 从两处 UI 移除该项；保留 JSON 字段并注明其为零值不变量 | `warehouse_registry.cpp:306` 把单位加价固定为 0 |
| G5 | 金融建筑四处半实现 | 见第 3 节 | 新增 `TestFinancialCapitalBacksBankLevels` |
| G6 | 文档称"有活动项目时拒绝省份转移"，代码实际是"失效化"；并存在零调用死函数 | 统一文档为失效化；删除 `Country::hasActiveConstructionForProvince` | 与 README 规则 7、`GUI_FRAMEWORK.md` 第 3 节一致 |
| G7 | 跨国路线守卫只在 World 命令层，存储层无校验 | `WarehouseNetwork` 接受 World 注入的国家解析器，`addRoute`/`addRailRoute` 直接拒绝跨国端点 | 无解析器的测试夹具行为不变 |
| H1 | 13 个构建必需文件与 `FUNCTIONAL_AUDIT.md` 未纳入版本控制，干净 clone 无法构建 | 全部纳入；并记录当前工作区状态使提交自洽 | 见第 4 节 |
| H2 | `dist/YehenalaMarket.exe` 是过时二进制，实测默认跑五市场夹具而非 69 市场标准世界 | 删除整个 `dist/`，`.gitignore` 改为忽略 `/dist/` | 直跑该二进制得到 `marketCount=5` |
| H3 | 无 CI | 新增 `.github/workflows/build.yml` 与本地入口 `tools/run_tests.ps1` | 本地入口已验证；CI 本身**尚未运行过** |
| H4 | `.gitignore` 未覆盖 `__pycache__/`、`*.pyc`、`*.bak` | 已补齐 | |
| H5 | `data/ne_110m_admin_0_countries.geojson` 无任何引用 | 已删除 | 全仓库 0 引用 |
| H6 | 无 LICENSE，却分发 exe 与 24.4 MB 字体 | 新增 MIT `LICENSE`；raylib / Natural Earth / 字体的授权各自独立说明，字体授权标注为待确认 | |
| D1 | `-` 按钮语义在三份文档中互相矛盾，其中两份自相矛盾 | **按确认结论保留拆除行为**，统一 5 处文档 | 代码与 `construction_ui_tests.cpp` 已固化为拆除 |
| D2 | 功能审核记录"4/4 测试通过"，实际注册 8 个 | 更新为 8/8，并补齐测试清单 | 本地实测 8/8 |
| D3 | 文档称 `AI_INTERVAL` 为 1，实际为 4；另有未文档化的上限 20 | 已更正并补记 `maxNewProjectsPerCountry = 20` | |
| D4 | 字体覆盖检查是单向的（只验证"目录 ⊆ 字体"） | 抽 `BuildRuntimeUiTexts()`，`main.cpp` 与测试共用；测试断言"目录 ∪ 运行时实体名 ⊆ 字体" | 注入运行时构造的 U+2000B 后测试以 `runtime-text` 失败 |
| D5 | 文本目录混入 300 条 raylib 字符串（约 19%） | 生成器跳过 `raylib`/`dist`/`tools`；目录由 1570 条降为 1278 条真实 UI 字面量 | |
| D6 | 生成器用**绝对路径**分量匹配跳过集 | 改为相对 `source_root` 匹配 | 在路径含 `build` 的 clone 中，修复前目录为空、`ui_text_catalog` 失败；修复后通过 |
| — | 零引用死常量 10 个、空实现函数 `syncFinanceCount`（5 处调用） | 已删除。`RAIL_KM_PER_TRANSIT_WEEK` 保留：它记录的是"已设计未启用"的意图，删掉会让文档失去指向 | |

`D6` 不是预先发现的问题，而是在做全新 clone 验证时才暴露出来的：CI 工作区或 git worktree 的路径通常含有 `build`，此时目录会静默为空。这条正是"只在本机路径下测试"所掩盖的缺陷。

## 3. G5：金融建筑完整实现

修复前四处半实现：

1. **金融区没有收入。** `revenueByBuilding[FINANCE]` 从未被赋值，因此文档所述"正净收益转入工商银行"是**不可达分支**，金融区长期亏损。
2. **储蓄银行没有独立职能。** 现金池被强制清零，只有"等级 = 投资池规模"这一显示性同步。
3. **银行等级由自身现金反推。** `level = ceil(现金 / 2500万)`，于是 `min(现金, 等级容量, 系统上限)` 中的等级容量恒 ≥ 现金，**该约束结构性永不生效**；同一函数还每周清零所有权，使银行利润分配成为空操作。
4. **金融业不计入 GDP。** 金融建筑无产出品，GDP 循环直接跳过它们，尽管它们支付工资并消耗原料；`BuildingTemplate::moneyMultiplier` 只写不读，黄金背书倍数被硬编码为 2.0。

实现方式（全部货币中性）：

- 借款人支付的利息被**拆分**：金融区取得 `FINANCE_INTERMEDIATION_FEE_SHARE`（25%）作为中介费，其余为工商银行利差。
- 银行持有**显式资本**：`level = ceil(资本 / BANK_LOAN_CAPACITY_PER_LEVEL)`；正利润的 10% 留存为资本，经营亏损与坏账核销由资本吸收；等级同步不再清零所有权。
- 储蓄银行按投资池规模与等级容量计算可中介存款，按存款利率计息，但**支付上限为其当期实际取得的利差**（因此不会抽干放贷能力），并按 `SAVINGS_PASS_THROUGH_SHARE` 按现金比例分配给各阶级，其余留存为资本。
- GDP 按收入法计入金融业增加值（劳动成本 + 正营业利润），**排除央行铸币收益**，以免货币发行本身抬高 GDP；黄金背书倍数改读模板。

104 周标准世界实测：GDP 总量约 +3.0%，金融建筑等级总和 +14%（1469 → 1673），**最高等级不变（92 → 93）**，说明保留-亏损反馈把规模控制在有界范围内。

## 4. 仓库可重建性验证

`git worktree` 从提交本身检出，独立目录配置、构建、运行全部测试：

- 修复前（`14ccd19`）：构建成功，但 `ui_text_catalog` **失败**（目录为空，缺字形"全"）——定位到 D6。
- 修复后（`97d4230`）：构建 100% 成功，**8/8 测试通过**。

这是"干净 clone 可构建"的唯一可信证据；仅在本机工作区构建会掩盖未跟踪文件与路径相关缺陷。

## 5. 仍未完成 / 需要决策

1. **GDP 稳定门槛的校准（需产品决策）。** 门槛已变为真实判定：`gdpCv104 <= 0.50`，在 104 周窗口内计算，**包含模型前 10 余周的启动爬坡段**。当前 69 个市场中 **37–38 个**未通过（中位数约 0.67，最大 2.58），因此标准世界无头报告退出码为 **2**。需要决定：调整阈值或统计窗口（例如只统计稳定段），还是修正启动期的产出爬坡。这是"门槛变得真实"的直接后果，不是回归。
2. **CI 尚未实际运行。** 工作流文件按本仓库真实的 MinGW/Ninja 工具链编写，但从未在 GitHub 上跑过，首次推送即为首次验证。
3. **金融业对长期经济平衡的影响需要观察。** 目前 104 周内指标有界且全部测试通过，但金融建筑中仍有较多利润率为负者（反映种子等级超过经济体的信贷需求）。若认为不合理，需要调整初始金融建筑等级或中介费率。
4. **`MingChinese.ttf` 的再分发依据仍未确认**（`LICENSE` 中已标注）。
5. **`README.md` 双语混排**是既有设计边界（FUNCTIONAL_AUDIT OUT-11），本次未处理。
